/*
1. Authentication, Authorization
2. login === sign in
3. register === sign up


*/
const bird = {
    size: 'small',
};

const mouse = {
    name: 'Mickey',
    small: true,
};

// console.log(mouse.bird.size);
// console.log(mouse[bird.size]);
console.log(bird["size"]);